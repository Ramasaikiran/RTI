# URL of the OTP backend (server/ folder)
VITE_API_URL=http://localhost:8787

# Optional: your own backend route for the draft agent (calls
# Claude/GPT server-side). Leave blank to use the built-in
# local template drafter — no key required.
VITE_DRAFT_API_URL=
